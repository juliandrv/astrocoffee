---
interface Props {
  icon: string;
  text: string;
}

const { icon, text }: Props = Astro.props;
---

<div class="py-10 space-y-4 flex flex-col items-center">
  <img src={icon} alt={`Icono `} class="max-w-52" loading="lazy" />
  <h3 class="text-2xl text-coffee-800 font-mono">{text}</h3>
</div>
